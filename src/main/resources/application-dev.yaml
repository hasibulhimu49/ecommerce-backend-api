spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: admin1234
    url: jdbc:postgresql://localhost:5432/e_commerce


    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update

      properties:
        hibernate:
          format_sql: true

